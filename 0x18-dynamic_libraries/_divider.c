#include <stdio.h>

/**
 * div - function to divide
 * @x: variable
 * @y: variable
 *
 * Return result
 */
int div(int x, int y)
{
	return (x / y);
}

/**
 * add - to add
 * @x: variable
 * @y: variable
 * Return: add
 */
int add(int x, int y)
{
	return (x + y);
}
